# 批量叶片 → CFD → AI 数据集

用户已确认上一轮提出的完整流程并要求执行。使用 executing-plans 流程连续实施；保留现有工作区和前序改动，不创建混合基准提交。

## 范围和接口

- 初始设计新增“批量方案”：以当前/已应用初设为基准，手动或真实大模型建议变量范围，LHS采样、真实几何校验、去重、有限补采样、预览和JSON导出。
- CFD新增同源后端“批次仿真”：导入/接收已校验参数包、冻结模板、限流补充队列、暂停/继续/取消、选择失败项重试。为CFD提交增加持久幂等键。
- AI独立工作区按批次扫描最新尝试，保留质检拒收、未提交记录、批次/几何/工况/任务关系。数据进入现有训练接口。
- 最多300组几何、10种工况、1000个算例；只支持现有二维冷态Pritchard模板。JSON导入再次执行几何和工况校验，不接受导入者声称的通过状态。
- 本机model-only可生成、导入和保存批次，真实求解须同源完整OpenFOAM后端。隔离夹具只验证调度，不写入真实CFD目录。没有外部API密钥时用兼容响应夹具测试模型合同，不伪称已调用真实模型。

## 执行记录

- [x] 1. 参数包合同、采样/导入校验和测试。
- [x] 2. 持久幂等提交、模板冻结、批次调度及重启/重试测试。
- [x] 3. 按批次构建数据集与质量反馈。
- [x] 4. 大模型批次建议接口，复用已配置服务。
- [x] 5. 初始设计、CFD、AI三处UI入口和跨页交接。
- [x] 6. 真实几何生成、隔离端到端验收、回归、文档、一次独立审阅。

每批保留基准、范围、随机种子、工况、来源与拒绝清单。AI训练只使用质量通过的真实CFD标签，不按性能优劣删除合格样本。

## 完成与验收记录

- 真实几何内核在浏览器生成12/12有效几何、3工况、36个待提交算例，预览后保存批次；model-only启动按钮禁用，交接到AI页成功。
- 隔离目录 `aeroblade/runtime/batch-ui-test` 中建立明确标记为“历史真实CFD回放验收（未新增求解）”的批次 `b329df5e81afe7581a09c37c0ec4360c`，只引用3个历史真实任务。原始CFD文件未改写。
- 浏览器按该批次提取数据集 `e18b1d80…`，质量通过3、训练样本3、独立几何3、拒收0；完成Ridge训练，模型卡 `df086624…`，训练2/验证1/测试0。该回放不等于新增真实仿真。
- 大模型批量建议复用配置服务，合同测试用明确的兼容响应夹具验证；本次未向用户外部模型服务发送实际测试请求。
- Final review: 4项Important，均已修复。导出再导入复用已验证参数包；缺失任务不阻止取消其余算例；控制响应与预览响应检查会话、所选批次、编辑版本和请求序号。对应Python与Node测试已观察RED→GREEN。
- 浏览器另发现“任务完成晚于目录读取，新数据集暂时不可选”的时序问题；完成任务产物与目录合并，新增2个前端回归测试，通过后重新验收训练交接。
- 最终回归：105/105个Windows兼容Python测试、19/19个Node测试通过，浏览器控制台无错误。POSIX专用 `test_pipeline` / `test_scheduler` 未计入本机套件；真实OpenFOAM批量求解仍待在完整后端执行。
- 本机已有3组几何的全局数据额外重建Ridge/ExtraTrees/MLP实验模型以匹配新代码版本，保留旧产物。最新模型：`6061965b8cb24fad9a8abab8`、`3805cedaa3444d6dbb317929`、`3218d2f25d8440a2a2b6fc3d`。
- 文档：`aeroblade/docs/BATCH_PRODUCTION.md`，README和旧批量脚本指南已更新。保留前序未提交改动；无真实求解任务自动启动。
