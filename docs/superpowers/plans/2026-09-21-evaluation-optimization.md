# CFD数据 → 参数回归 → 评估页面 → 优化闭环 实施计划

**Goal:** 将真实CFD的质量检查和六项指标、标量回归、统一评估界面及可取消的优化调度接通。
**Architecture:** 新建 evaluation 包；计算在独立Python worker中执行，HTTP服务只鉴权、冻结输入和调度。模型与数据带内容摘要，AI结果和CFD证据分开保存。
**Scope:** 二维冷态Pritchard叶栅；Ridge/ExtraTrees/小型MLP标量预测。无三维、整级效率、FEA；PyG/FNO仍在原AI计划中待实施。

## Rulings / 执行记录

- Ruling: 按用户新指定四步执行，不将原AI计划里的图网络/FNO作为本轮前置条件——先打通低成本标量闭环——复杂流场模型仍需后续独立实现。
- Ruling: 在已有工作目录定向修改并保留前序未提交改动——当前用户使用的完整平台含登录/模型改造——不创建掺入这些改动的基准提交。
- Ruling: 当前真实数据仅3组独立几何，只允许实验性smoke训练与显式标记预测；不声称泛化精度。优化须区分AI候选与CFD复核。
- Ruling: WSL列举超时，原生Windows不能执行现有POSIX求解流程；软件自动闭环用受控夹具测试，真实CFD提交只在manager健康检查通过时启用。

## Tasks

- [x] 1. 只读ASCII网格/边界场解析、实际厚度/法向、可追溯热物性、六指标；解析场和正负符号测试。
- [x] 2. 审计器增加只读入口；数据质量分层、时间一致性、摘要与去重、拒绝报告；对现有数据真实扫描。
- [x] 3. 参数/工况特征、按几何分组划分、训练集归一化、Ridge/ExtraTrees/MLP、模型卡及范围提示；真实小样本smoke。
- [x] 4. 独立worker任务API、持久状态与重启中断处理；AI与CFD评估记录使用冻结快照，匹配工况及几何。
- [x] 5. DOE/代理筛选/CFD复核优化流程、目标约束、预算、暂停/继续/取消、Pareto与候选回写；失败与非法几何保留记录。
- [x] 6. 评估与优化工作区连接数据、训练、预测、优化和候选对比；新输入使旧显示明确过期。
- [x] 7. 回归、真实数据结果与浏览器验收、打包文档；一次独立审阅的重大问题已通过失败测试复现并修复。

验收不包含伪造CFD、扩充样本或外部训练结果。所有演示/夹具与真实数据单独标识。

## 完成记录（2026-09-21）

- Tasks 1–2: complete。真实数据集 `4a1c65aef59fe3917fa8ea37`：23任务，5个workflow通过，18拒收，去重3样本/3几何，0工程级。源数据只读；实际网格展宽10mm。
- Task 3: complete。真实训练模型：Ridge `832be7b595464bb89771c1a5`、ExtraTrees `c305b86091a54154a0a76fdb`、MLP `0d4b55d31bab42a4bf233967`。均训练2/验证1/测试0，模型卡及JSON权重可恢复预测；无正式精度排名。
- Tasks 4–6: complete。受鉴权保护的任务API、独立worker、持久状态、受控闭环、三个页签与候选回写已接通。窗口与窄屏浏览器验收包括真实预测、历史快照标记；隔离夹具验证优化4次预算、候选轮廓与回写。
- Final: fixed Important——不同CFD模板/热物性混排、旧代理筛选：`test_mixed_cfd_context_is_excluded_from_ranking` 和 `test_surrogate_context_mismatch_stops_after_baseline` RED→GREEN。基准先求解并冻结上下文；后续不匹配CFD拒收、旧代理停止。
- Final: minor (deferred): worker已完成数值计算、尚未写入最终状态的极短窗口里接受取消，最终可能显示completed；不影响结果，也未发现遗留长任务。保留为后续状态语义改进。
- Final verification: Windows兼容Python套件88/88；随后新增同上下文代理成功路径，相关套件8/8（累计89个不同Python用例通过）；Node 14/14；前端语法和 `git diff --check` 通过；85文件便携包包含evaluation源码/依赖说明，不含私有数据。POSIX脚本测试 `test_pipeline` / `test_scheduler` 因本机环境不适用未计入。
- Task 7: complete。独立审阅仅1项Important，已修复；操作与部署文档 `aeroblade/docs/EVALUATION_OPTIMIZATION.md`。8791服务按原model-only方式重启，账号及模型配置数据库保留。
- Working tree: 保留前序未提交变更，未创建提交或合并；未删除任何真实CFD、数据集或模型。测试任务与进程独立隔离，夹具服务已关闭。

下一步需要真实OpenFOAM后端完成新增CFD优化运行，并扩展独立几何数据；PyG/FNO、三维验证、网格独立性和工程级验证不在本次完成范围。

## 用户修订：AI 为独立一级工作区

- AI 是平台主要特色，不能收进评估页。顶部固定保留五个入口：初始设计、叶片设计、CFD仿真、AI预测、评估与优化。
- AI预测集中提供代理模型推理、CFD数据与训练、模型卡，以及大模型配置/总师智能体和后续PyG/FNO路线；已有大模型对话仍由总师智能体承载。
- 评估与优化保留CFD指标对比和自动优化；共享当前几何、工况、代理模型与任务，跨工作区切换不重置输入或中断计算。
- 本次调整复用已有后端，不改变数据合同和模型权重；新增/待开发能力明确区分。
- 验收完成：五个一级入口同时可见；隔离账号从AI页面完成真实Ridge训练（模型 `320159cb`）和预测；工况100210 Pa同步到评估页面并正确标记历史预测；大模型配置弹窗可由AI页面打开。既有14项Node测试、脚本语法检查通过。
